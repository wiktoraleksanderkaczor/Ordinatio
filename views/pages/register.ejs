<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="stylesheet" type="text/css" href="styles/style.css"/>
		<title>Ordinatio</title>
	</head>
	<body>
		<div>
			<h1>Ordinatio Admin Page</h1>
		</div>
		<div>
			<h1> Employee Registeration Form </h1>
				<form id="registerForm" method="POST" action="/register">
				<label for="email">Email address:</label>
				<input type="text" name="email" placeholder="Email Address" pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" title="Please enter a valid email address" required>
				<label for="password">Password:</label>
				<input type="password" name="password" placeholder = "Password"  pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Password must be at least 8 characters long, and include an upper case letter, a lower case letter and a number." required>
				<label for="verify">Verify password:</label>
				<input type="password" name="verify" placeholder = "Verify password"  pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Password must be at least 8 characters long, and include an upper case letter, a lower case letter and a number." required>
				<label for="firstName">First Name:</label>
				<input type="text" name="firstName" placeholder = "First name" pattern="[a-zA-Z -]" title="Please enter employee's first name" required>
				<label for="surname">Surname:</label>
				<input type="text" name="surname" placeholder="Surname" pattern="[a-zA-Z -]" title="Please enter employee's surname" required>
				<label for="jobTitle">Job title:</label>
				<input type="text" name="jobTitle" placeholder="Job Title" pattern="[a-zA-z ]" title="Please enter employee's job title" required> 
				<label for="role">Type of user:</label>
				<select name="role" required>
					<option value="admin">Admin</option>
					<option value="employee" selected>Employee</option>
				</select>
				<input type="submit" value="Register">
			</form>
			<form id="linksForm" method="GET" action="/main">
				<input type="submit" value="Go back" formaction="/main" form="linksForm">
			</form>
			<h1><%= info %></h1>
		</div>
	</body>
</html>
